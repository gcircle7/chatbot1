# docker-compose 가 읽는 루트 환경변수
# 복사 후 실제 값 입력: cp .env.example .env

# 호스트 MySQL 접속 주소
#   Windows (Docker Desktop) : host.docker.internal  (기본값, 변경 불필요)
#   Linux   (CentOS/Ubuntu)  : 172.17.0.1
MYSQL_HOST=host.docker.internal
MYSQL_PORT=3306
